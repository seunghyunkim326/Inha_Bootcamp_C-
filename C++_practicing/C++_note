전처리기 : 내가 만든 코드 컴터는 이해못해 -> 컴파일러가 2진수로 변환        컴파일러가 메인 작업 전 하는 작업
전처리기에서 잘못되면 컴파일러가 오류를 반환하지 못해서 매우 힘들다 -> 건들지 마라
#포함된 구문 => 전처리기와 관련된 구문
using namespace std; => 컴파일에게 std라는 namespace를 계속 사용할테니 생략해도 알아서 하라는 뜻
// => 주석처리    또는    /**/ => *안의 값들 주석처리
변수 타입 항상 명시!!   타입에 대해서 엄격!
double => floating point 설정 시 사용
const => 상수 선언시 사용   바꿀 수 없는 값이므로 처음에 선언과 동시에 값 지정해야함
추후 변경 불가능 => 선언 시 초기화 안하면 쓰레기 코드
cplusplus.com/reference/~~~ 활용
void : return값 없을 때
